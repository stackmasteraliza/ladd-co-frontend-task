export type AdditionalService = {
  title: string;
  description: string;
};

export type NumberedService = {
  number: string;
  title: string;
  description: string;
};

export type Location = {
  city: string;
  country: string;
};
